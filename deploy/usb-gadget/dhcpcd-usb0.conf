# Use this only if dhcpcd is the active network manager on the Pi
# (systemctl is-active dhcpcd -- see deploy/README.md). If NetworkManager is
# active instead, use nm-usb0.sh.
#
# Append this stanza to /etc/dhcpcd.conf:

interface usb0
static ip_address=10.55.0.1/24
nohook wpa_supplicant
